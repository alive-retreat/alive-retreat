{
  "name": "retreat-alive",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "vite": "^7.3.1"
  }
}
